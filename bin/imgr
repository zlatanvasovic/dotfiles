#!/bin/bash
#
# Resize images in the current directory.
#
# Usage:
#
#   $ imgr FORMAT WIDTH
#

set -e

for img in *.$1
do
  if ! [[ $img == *-resized* ]]
  then
    convert $img -resize $2 ${img%%.*}-resized.${img#*.} > /tmp/imgr.log
  fi

  printf "\n  \033[1;32mDone.\033[0m\n\n"
done
