#!/bin/bash
#
# Update great things.

# Root privilegies is needed for updates
if [[ $EUID -ne 0 ]]
then
  echo Script must be run as root! 1>&2
  exit 1
fi

# Avoid cache in /home
cd /tmp

# Updt!
apt-get update
apt-get upgrade -y
npm up -g
