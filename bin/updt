#!/bin/bash
#
# Update packages.

set -e

if [ $(which apt-get) ]
then
  sudo apt-get update && sudo apt-get upgrade -y
fi

if [ $(which npm) ]
then
  sudo npm update -g
fi

if [ $(which gem) ]
then
  sudo gem update
fi

# Remove temporary update files
sudo rm -rf ~/tmp
