#!/bin/bash
#
# Count number of code lines.

ignores=(.git)
ignore=""
for item in ${ignores[*]}
do
  ignore="$ignore -o -name $item -prune"
done

find . -type f -print $ignore | xargs wc -l | awk 'END {print $1}'