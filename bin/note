#!/bin/bash
#
# Simple notes app.
#
# Usage:
#
#   $ note
#   $ note add NOTE
#   $ note rm NOTE
#

case "$1" in
  add)
    touch ~/dev/txt/note/$2
    ;;
  rm)
    rm ~/dev/txt/note/$2
    ;;
  *)
    ls -1 ~/dev/txt/note
esac
