#!/bin/bash
#
# Simple todo app.
#
# Usage:
#   $ todo add TASK
#   $ todo rm TASK
#   $ todo ls
#   $ todo clear

if [ $1 = add ]
then
  touch ~/dev/todo/$2
fi

if [ $1 = rm ]
then
  rm ~/dev/todo/$2
fi

if [ $1 = ls ]
then
  ls -1 ~/dev/todo
fi

if [ $1 = clear ]
then
  rm ~/dev/todo/*
fi