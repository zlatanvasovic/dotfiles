#!/bin/bash
#
# Simple todo app.
#
# Usage:
#   $ todo ls
#   $ todo (add|rm) TASK

if [ "$1" = ls ]
then
  ls -1 ~/dev/txt/todo
fi

if [ "$1" = add ]
then
  touch ~/dev/txt/todo/$2
fi

if [ "$1" = rm ]
then
  rm ~/dev/txt/todo/$2
fi