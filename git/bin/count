#!/bin/bash
#
# Count Git commits or branches.
#
# Usage:
#
#   $ git count [b|ca]
#

case "$1" in
  # Branches
  b)
    git branch | wc -l
    ;;

  # Commits by author
  ca)
    git shortlog -sn
    ;;

  # Commits
  *)
    # Reserved for newer versions
    git rev-list HEAD --count
esac
